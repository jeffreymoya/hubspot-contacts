session.cookie_httponly = 1  ; Prevent JavaScript from accessing session cookies
session.cookie_secure = 1    ; Ensure cookies are sent over HTTPS only
session.cookie_samesite = Lax; Mitigate CSRF risks
session.use_strict_mode = 1  ; Prevent attacks on session management
session.use_only_cookies = 1 ; Only use cookies for sessions
session.use_trans_sid = 0    ; Disable URL-based sessions
